<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bonjour</title>
    <link rel="icon" href="/assets/images/favicon.png">
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>

<body>
    <h1>Bonjour {{ name }} !</h1>
    <h1>Instruments</h1>
<ul>
    {% for product in product |slice(0, 2) %}
        <li>{{ product }}</li>
    {% endfor %}
</ul>
<ul>
{% for product in product |slice(3, 6) %}
        <li>{{ product }}</li>
    {% endfor %}
</ul>
 {{ dump(product) }}
13</body>
14</html>
